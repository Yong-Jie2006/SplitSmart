# PostgreSQL connection string. Add the real value to a local .env file.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
# A separate disposable database used only by Playwright. Never point this at production data.
DATABASE_URL_TEST=postgresql://USER:PASSWORD@HOST:5432/SPLITSMART_TEST?sslmode=require
